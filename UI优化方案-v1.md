# 习惯打卡 · UI 优化方案 v1

> 版本：v1.0（2026-08-16）
> 范围：微信小程序（`weapp/src`，Taro 4 + React 18 + TS + SCSS）
> 状态：备案审核期规划稿，备案通过前完成方案与优先级，通过后按版本实施
> 审读依据：`today/calendar/stats/manage` 四个页面 + `app.scss` + `utils/*`（约 1800 行）

---

## 0. 一句话结论

**底子是好的**（主题系统、卡片设计、交互动效都用心），**问题集中在三层**：① 数据层有真实风险（全量写入、无迁移、直接 mutate）；② 交互层缺可访问性与误触防护；③ 视觉层缺一致性与"卖点"（统计趋势、打卡彩蛋）。按 P0 → P2 分三个小版本推进，每个版本验证命令：`npx tsc --noEmit` + `npm run build:weapp`。

---

## 1. 现状优点（保留，不要动）

- **6 主题 CSS 变量体系**（`app.scss`）：设计规范、切换流畅，是项目亮点
- 卡片式布局 + 毛玻璃（backdrop-filter）+ 柔和阴影，风格统一
- 打卡按钮的 popCheck 弹性动画、列表 slideIn 入场，细节到位
- 反馈走云函数（feedbackCollect）且有本地兜底，链路正确
- 代码量小（~1800 行），重构空间大

---

## 2. P0 · 数据安全与功能缺陷（必须先修）

### P0-1 打卡数据全量读写 localStorage ⚠️ 高
- **位置**：`utils/storage.ts`（`STORAGE_KEY='habit_data'` 单 key 存全部）+ `pages/today/index.tsx:47`
- **问题**：每次打卡把**整个数据结构**（含全部历史 checkins）序列化重写；习惯多、打卡久后单次写入越来越大，接近 localStorage 5MB 上限时**写入失败 = 打卡丢失**。
- **建议**：
  - 拆分存储：`habit_data`（习惯列表+主题，小）与 `habit_checkins_<id>`（每习惯独立打卡记录）分开；
  - 或至少加**写入失败兜底**（try/catch + wx.showModal 提示"存储已满"）。
- **验证**：构造 10 个习惯 × 365 天数据，连续打卡 100 次无失败、无卡顿。

### P0-2 toggleCheckin 重复读取 + 直接 mutate ⚠️ 高
- **位置**：`pages/today/index.tsx:31-49`
- **问题**：`saveData({ habits, theme: loadData().theme })` 先 `loadData()` 再 `saveData()`（一次打卡两次读）；且直接修改 `h.checkins`（可变修改），`setState({ habits: [...habits] })` 只浅拷贝——React 严格模式下可能不重渲染，且容易引入并发覆盖。
- **建议**：改为不可变更新：`const next = habits.map(x => x.id === id ? { ...x, checkins: { ...x.checkins, [td]: !checked } } : x)`，一次读、一次写。

### P0-3 自定义导航栏的安全区适配 ⚠️ 高
- **位置**：`app.config.ts:9`（`navigationStyle: 'custom'`）+ `app.scss:81`（`padding: 160px 24px 140px` 写死）
- **问题**：胶囊/状态栏高度因机型而异，写死 160px 在刘海屏/不同机型上可能遮挡标题或留白过大。
- **建议**：用 `env(safe-area-inset-top)` + 胶囊高度动态计算（Taro 的 `Taro.getWindowInfo()` / `Taro.getMenuButtonBoundingClientRect()`），或改用 Taro 的 `<SafeArea>` 方案，把顶部 padding 改为 CSS 变量注入。

### P0-4 单行输入用了 Textarea ⚠️ 中
- **位置**：`pages/manage/index.tsx:222`（习惯名称）、`:201`（联系方式）
- **问题**：Textarea 原生可换行（回车键），单行输入场景体验不对（名称里出现换行符）。
- **建议**：改用 `<Input>`（`type='text'`），保留 maxlength 与字数统计。

### P0-5 数据无版本迁移机制 ⚠️ 中
- **位置**：`utils/storage.ts`
- **问题**：将来加字段（如归档、目标天数、提醒开关）时，旧数据无法平滑升级。
- **建议**：`habit_data` 增加 `schemaVersion` 字段 + 迁移函数链（migrate(v1→v2)），读取时自动升级。

### P0-6 图标资源冗余 ⚠️ 低
- **位置**：`src/assets/icons/` 下每个图标 `.png` + `.jpg` 双份（如 `coffee.png`/`coffee.jpg`）
- **建议**：确认只用一个格式，删除另一份，减小包体。

---

## 3. P1 · 交互与可访问性（体验升级）

### P1-1 打卡按钮不是 Button、无防误触 ⚠️ 高
- **位置**：`pages/today/index.tsx:101-106`
- **问题**：打卡用 `View` + `onTouchEnd`——屏幕阅读器读不到、无键盘焦点；滑动列表时手指抬起可能误触打卡。
- **建议**：改 `<Button>`（或加 `role="button"`/`aria-label`）；手势判定：`onTouchStart` 记录坐标，位移超过阈值不触发（防滚动误触）。

### P1-2 打卡成功反馈太弱 ⚠️ 中
- **位置**：`pages/today/index.tsx:44`（仅 `wx.showToast`，且全是 `icon:'none'`）
- **问题**：Web 版有"彩蛋庆祝"，小程序版只有一行 toast；toast 一闪而过，成就感不足。
- **建议**：补一个全屏轻量彩蛋（emoji 粒子/弹跳动画，CSS 实现，500ms 内结束不打扰）；Toast 成功场景用 `icon:'success'`。

### P1-3 删除无撤销 ⚠️ 中
- **位置**：`pages/manage/index.tsx:84-98`
- **问题**：删除确认后数据即失，误删无法恢复。
- **建议**：删除时先把记录快照存入 `habit_trash_<id>`（带时间戳），toast 带"撤销"操作；或至少提供"最近删除"恢复入口。

### P1-4 日历信息缺失 ⚠️ 中
- **位置**：`pages/calendar/index.tsx:80`
- **问题**：日期格只显示打卡次数（>1 才显示数字），看不出"哪天打了哪些习惯"；点击日期无详情。
- **建议**：点击日期弹出当日明细（哪些习惯完成）；单习惯完成时在格子里显示该习惯 emoji 而非空点。

### P1-5 主题切换入口太深 ⚠️ 低
- **位置**：`pages/manage/index.tsx:151`（管理页第三个按钮）
- **问题**：新用户找不到换主题的入口；首次使用无引导。
- **建议**：首次启动（无习惯数据时）引导用户选主题；今日页顶部加主题快捷入口（或放设置弹层）。

---

## 4. P2 · 视觉与一致性（打磨）

### P2-1 页面标题风格不统一
- today 用 `page-header-compact`（标题+日期徽章），其他页用 `page-title`（左竖条）。统一为一种标题组件（建议保留 today 的紧凑型，全局一致）。

### P2-2 统计页缺"趋势感"
- 当前只有静态数字 + 当月完成率条。建议加：近 7 天 / 近 30 天完成率 mini 条形图（纯 CSS/轻量 canvas，不引库），这是习惯类产品最有粘性的部分。

### P2-3 空状态可更可爱
- 三个空状态文案/图标已不错（🌸📋📊），可统一组件化（`EmptyState`），加一句鼓励语和"去看看"行动按钮。

### P2-4 动效一致性
- 已有 popCheck/slideIn/pageEnter；建议补：主题切换时的过渡（背景渐变更明显）、列表项删除时的消失动画（现在直接消失）。

---

## 5. 实施计划

| 版本 | 内容 | 风险 | 预计改动 |
|---|---|---|---|
| **v1.1 数据加固**（先做） | P0-1~P0-6 | 低（改动集中 storage + today） | ~6 处 |
| **v1.2 交互升级** | P1-1~P1-5 | 中（打卡交互重写） | ~8 处 |
| **v1.3 视觉打磨** | P2-1~P2-4 | 低 | ~6 处 |

**每个版本的标准流程**：
1. 本方案细化成任务清单（DSH 产出）
2. 实施（DSH 改代码）
3. 验证：`npx tsc --noEmit` + `npm run build:weapp` 全绿
4. **外部检查**：代码交给 Codex 审（用《多模型外部检查协作方案》的指令模板），意见落 `协作/codex-审查-日期.md`
5. 修完意见 → 你开微信开发者工具真机/模拟器验收
6. 更新 `PROJECT_HANDOFF.md` 交接文档

**建议**：备案通过前完成 v1.1（数据加固，无 UI 变化，最安全）；v1.2/v1.3 视备案进度并行推进。

---

## 6. 验收标准（v1.1 为例）

- [ ] 连续打卡不因存储增长变慢/失败（10 习惯 × 365 天压测）
- [ ] 切换页面主题保持正确、无闪烁
- [ ] 微信开发者工具 + 真机（刘海屏机型）标题不遮挡
- [ ] `npx tsc --noEmit`、`npm run build:weapp` 全绿
- [ ] 旧数据（现有 habit_data）读取正常，无迁移报错

---

## 7. 待确认事项

- [ ] 打卡数据是否要上**云同步**（当前纯本地，换机即丢）——产品决策，影响 P0-1 存储方案选型
- [ ] 备案通过后是否保留"体验版自测"窗口做 UI 验收
- [ ] 是否需要把 Web 版（`习惯打卡.html`）与小程序 UI 对齐（Web 版已有彩蛋，可作为小程序 P1-2 的参考实现）
