# 习惯打卡 - 云开发避坑与最佳实践

## 2026-08-19 复盘（认证/版本/协作防坑）

### 当前状态

- 微信认证已通过，名称搜索已恢复（另一个微信可搜索到小程序）。
- v1.1.0 已提交审核；线上仍为 1.0.0。
- 手机打开显示“开发版”是扫了开发工具预览码，不是线上版。

### 避坑（新增）

1. **开发版 ≠ 线上版**：小程序有 开发版/体验版/审核版/线上版 四种。扫开发工具二维码打开的是开发版；普通用户看到的是线上版。验证线上版要从微信名称搜索进入，不要扫预览码。
2. **认证通过 ≠ 版本审核通过**：认证解决的是“能不能被搜到”，版本审核解决的是“新代码能不能上线”。认证过了不点「发布」，线上还是旧版。
3. **版本发布动作不能少**：审核通过后必须在 `mp.weixin.qq.com` → 版本管理 → 审核版本 → 点「发布」，新版本才会成为线上版。
4. **会话切模型报 400/1261**：长会话 + time-context 等注入内容可能把 prompt 顶过新模型上下文上限（识图模型窗口通常更小）。切回大上下文模型、压缩/清空上下文或新开会话即可。
5. **git push 卡死**：`git push` 可能因网络问题挂起数分钟；可加 `GIT_HTTP_LOW_SPEED_LIMIT=1000 GIT_HTTP_LOW_SPEED_TIME=10 git push origin main` 快速失败或成功，避免干等。

### 最佳实践（新增）

1. 每次版本发布/认证/审核状态变化，当天同步到 `PROJECT_HANDOFF.md` 并 push，保证新会话和协调方看到最新状态。
2. 线上验证路径固定为：微信搜索名称 → 进入 → 管理页看版本号。
3. 审核通过当天立即发布，避免线上旧代码长期暴露数据风险。


## 2026-08-06 复盘

### 当前状态

- ICP：小程序备案电话仍在等待；域名过初审的是另一个项目，不和小程序混记。
- 云环境：`cloudbase-d3g7noa8yac9d743c` 已确认存在。
- 云函数：`feedbackCollect` 已部署且 Active；`dailyReminder` 尚未部署。
- 数据库：`feedbacks` 集合已有 4 条测试记录，状态均为 `new`，反馈链路正常。

### 正确入口

1. CloudBase 桌面控制台：
   `https://tcb.cloud.tencent.com/login`
   - 必须选择「使用微信公众平台账号登录」
   - 不能选普通「腾讯云账号登录」
   - 扫码后选择对应小程序，才能看到微信开发者工具里创建的环境
2. 微信公众平台后台：
   `mp.weixin.qq.com` → 云服务 → 云开发
3. 微信开发者工具 CLI：
   ```bash
   cli cloud env list --project /Users/mac/Documents/习惯打卡测试/weapp
   cli cloud functions list --project /Users/mac/Documents/习惯打卡测试/weapp --env cloudbase-d3g7noa8yac9d743c
   ```

## 避坑

1. 新版微信开发者工具的「工具栏管理」里不一定有云开发选项；顶部未命名图标可能是账号头像，不是云开发入口，不要再从那里找。
2. 微信公众平台「管理环境」打开的是官方云开发小程序二维码，不是桌面网页数据库；查看集合和记录请用 CloudBase 桌面控制台。
3. CloudBase 用普通腾讯云账号登录时可能显示 0 个环境，这是登录方式不对，不是环境未创建。
4. CLI 查询环境/函数需要传入 `--env`，且会写微信开发者工具配置目录；只读操作也应使用已授权命令。
5. `dailyReminder` 不能带着 `YOUR_TEMPLATE_ID` 部署；先配置订阅消息模板 ID，再部署。

## 最佳实践

1. 把环境 ID、登录方式和关键链接固定写进项目文档，避免每次从界面里重新找。
2. 环境/函数状态优先用 CLI 查询；数据库记录用官方 CloudBase 控制台确认。
3. `feedbacks` 权限保持「仅创建者可读写」，保留 `openid`、`status`、`text` 等字段便于排查。
4. 小程序备案和域名备案分开记录，不能因为其中一个过初审就认为另一个已完成。
5. 提醒函数部署前确认 `reminders` 集合存在，并完成订阅消息模板配置。

## 2026-08-19 第二轮复盘（宠物/悬浮/删除/目标）

### 避坑（新增）

1. **Taro 小程序跨 Tab 全局悬浮层不可靠**：App 级 fixed 层在不同 Tab 页表现不一致，常出现“切页后不显示”。稳妥做法是**每个 Tab 页各自挂载悬浮组件**，位置存本地存储实现跨页一致。
2. **悬浮宠物位置要持久化**：把 `left/top` 存 `habit_pet_pos`，页面切换后从存储读取，避免每次从右下角重新出现。
3. **飞入动画只在召唤时播**：给悬浮组件加 `animate` 参数，宠物页召唤时传 `true`，其他页面默认 `false`，避免每次切页都播放动画。
4. **setTimeout + React state 异步导致删不掉**：多个删除倒计时不能只存一个 pendingDelete，必须用“多个 id + 多个定时器”，删除用函数式 `setState(prev => ...)` 从最新列表过滤。
5. **能量/孵化值规则要直观**：用户会误以为“7 点=7 个习惯”。界面必须写清“能量是累加值：新建习惯+1、每日首卡+1”，否则解释成本很高。
6. **宠物数据要版本隔离**：`PetData` 加 `schemaVersion`，后续字段变更只升版本，旧数据读入时做默认值兜底，不破坏已孵化用户。
7. **目标示例按身份过滤**：小目标/里程碑不要一次性全展示，按用户身份（英语/考研/认知/生活）显示对应分类，提供“展开全部”。

### 最佳实践（新增）

1. 悬浮宠物位置：`drag end` 时才写存储，避免拖动过程高频写。
2. 动画与位置分离：位置持久化，动画一次性，状态切换不重播。
3. 多定时器管理：`Record<number, timer>`，组件卸载时统一清理。
4. 每个新增功能当天同步 `PROJECT_HANDOFF.md` 并 push，作为多模型协作入口。
