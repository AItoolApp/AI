# 习惯打卡 - 云开发避坑与最佳实践

## 2026-08-06 复盘

### 当前状态

- ICP：小程序备案电话仍在等待；域名过初审的是另一个项目，不和小程序混记。
- 云环境：`cloudbase-d3g7noa8yac9d743c` 已确认存在。
- 云函数：`feedbackCollect` 已部署且 Active；`dailyReminder` 尚未部署。
- 数据库：`feedbacks` 集合已有 4 条测试记录，状态均为 `new`，反馈链路正常。

### 正确入口

1. CloudBase 桌面控制台：
   `https://tcb.cloud.tencent.com/login`
   - 必须选择「使用微信公众平台账号登录」
   - 不能选普通「腾讯云账号登录」
   - 扫码后选择对应小程序，才能看到微信开发者工具里创建的环境
2. 微信公众平台后台：
   `mp.weixin.qq.com` → 云服务 → 云开发
3. 微信开发者工具 CLI：
   ```bash
   cli cloud env list --project /Users/mac/Documents/习惯打卡测试/weapp
   cli cloud functions list --project /Users/mac/Documents/习惯打卡测试/weapp --env cloudbase-d3g7noa8yac9d743c
   ```

## 避坑

1. 新版微信开发者工具的「工具栏管理」里不一定有云开发选项；顶部未命名图标可能是账号头像，不是云开发入口，不要再从那里找。
2. 微信公众平台「管理环境」打开的是官方云开发小程序二维码，不是桌面网页数据库；查看集合和记录请用 CloudBase 桌面控制台。
3. CloudBase 用普通腾讯云账号登录时可能显示 0 个环境，这是登录方式不对，不是环境未创建。
4. CLI 查询环境/函数需要传入 `--env`，且会写微信开发者工具配置目录；只读操作也应使用已授权命令。
5. `dailyReminder` 不能带着 `YOUR_TEMPLATE_ID` 部署；先配置订阅消息模板 ID，再部署。

## 最佳实践

1. 把环境 ID、登录方式和关键链接固定写进项目文档，避免每次从界面里重新找。
2. 环境/函数状态优先用 CLI 查询；数据库记录用官方 CloudBase 控制台确认。
3. `feedbacks` 权限保持「仅创建者可读写」，保留 `openid`、`status`、`text` 等字段便于排查。
4. 小程序备案和域名备案分开记录，不能因为其中一个过初审就认为另一个已完成。
5. 提醒函数部署前确认 `reminders` 集合存在，并完成订阅消息模板配置。
