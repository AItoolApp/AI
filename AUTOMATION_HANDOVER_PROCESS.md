# 线程交接与自动化审计流程

> 用途：Codex 线程换线、项目会议继承、定时任务缺失排查时，其他项目可直接按本流程操作。
> 原则：先确认，再交接，只汇报缺口；修复动作等用户确认后执行。

## 一、触发场景

- 用户说“承接/换线程/会话数据丢失”时。
- 项目独立会话被归档、重开或迁移时。
- 早/晚会、提醒、日报等自动化目标线程变化时。
- 需要审计交接摘要、会议和定时任务是否缺失时。

## 二、确认当前线程

1. 用 `list_threads` 或会话元数据确认当前 Codex 线程 ID。
2. 检查该线程是否为项目对应独立会话，工作目录是否正确。
3. 命名按 `YYYY-MM-DD 项目：任务`，例如 `2026-08-07 习惯打卡：交接与早晚会承接`。

## 三、确认项目自动化归属

1. 读取 `/Users/mac/.codex/automations/*/automation.toml`。
2. 按项目名、prompt、`target_thread_id` 找到对应自动化。
3. 对比旧目标线程和新线程：
   - 相同：说明已经继承，不需要改。
   - 不同：用 `automation_update` 更新 `targetThreadId`，保留 `rrule`、`status`、prompt。
4. 更新后复核 TOML 中的 `target_thread_id` 和 `status`。

## 四、同步协调线程

1. 向协调线程 `019fdad3-7b3e-7280-83bb-347a73149ce2` 发送换线说明。
2. 内容必须包含：
   - 项目名。
   - 自动化 ID。
   - 旧线程 ID。
   - 新线程 ID。
   - 是否已切换，运行时间是否保持不变。
3. 如协调线程不可用，按 `早晚会抄送/README.md` 兜底写入日期文件。

## 五、审计定时任务

### 1. Codex 自动化

```bash
cat /Users/mac/.codex/automations/*/automation.toml
```

检查项：

- `status` 是否为 ACTIVE 或 PAUSED，是否符合预期。
- `rrule` 是否覆盖需要的会议时间。
- `COUNT` 是否为一次性任务，是否已经过期。
- `target_thread_id` 是否指向当前活跃线程。

### 2. launchd 定时任务

```bash
launchctl print gui/$(id -u)/<label>
```

检查项：

- 服务是否存在。
- 状态是否为 running / not running。
- 最近 exit code 是否为 0。
- plist 是否在 `~/Library/LaunchAgents/` 或显式加载。

### 3. 运行日志

```bash
python3 /Users/mac/Documents/问题和学习/check_status_runs.py
```

检查项：

- 最近 7 天是否有缺失日期。
- 当天实际运行时间是否接近计划时间。
- 日志显示成功但服务未加载时，说明可能是临时运行，持久调度仍缺失。

## 六、输出缺口清单

记录以下信息：

- 项目名。
- 缺口类型：交接摘要、早会、晚会、日报、提醒、launchd、文档不一致。
- 当前实际状态。
- 建议动作。
- 是否等待用户确认。

## 七、2026-08-07 参考案例

### 已处理

- 习惯打卡 `automation-3` 目标从旧线程切到新线程。
- 新线程按 `YYYY-MM-DD 项目：任务` 命名。
- 已向协调线程发送换线说明。

### 待用户确认

- 商业洞察 `com.businessinsight.daily` 未加载，调度器 PID 已死。
- AI 动漫 `ai` 自动化只有 16:00 晚会，缺 7:00 早会。
- 万物生缺独立交接摘要和早/晚会自动化。
- 习惯打卡 `11-30` 为 `COUNT=2` 一次性提醒，可能已过期。
- 共享文档中 AI 动漫“每天 7:00/16:00”与实际只配 16:00 不一致。

## 八、完成标准

- 自动化目标线程正确。
- 协调线程已收到换线说明。
- 项目交接文档已记录新线程 ID。
- 所有缺口已列出并标为“待用户确认”或“已处理”。
- 未获得用户确认前，不自动修改其他项目自动化、launchd 或共享状态。
